import PartnerPage from "@/components/parentPages/PartnerPage";
import { metaData } from "@/lib/metadata";

export const metadata = metaData.home;

export default function Partners() {
  return (
    <div>
      <PartnerPage />
    </div>
  )
}
