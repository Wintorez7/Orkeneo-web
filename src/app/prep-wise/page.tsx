import Prep from "@/components/parentPages/Prep";
import { metaData } from "@/lib/metadata";

export const metadata = metaData.prepWise;

export default function Prepwise() {
  return (
    <Prep />
  );
}
