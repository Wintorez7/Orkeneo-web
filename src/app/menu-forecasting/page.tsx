import MenuForecastingComponent from "@/components/parentPages/MenuForecasting";
import { metaData } from "@/lib/metadata";

export const metadata = metaData.menuForecasting;

const MenuForecasting = () => {
  return (
    <MenuForecastingComponent />
  );
};

export default MenuForecasting;
