import LaborForecasting from "@/components/parentPages/LaborForecasting";
import { metaData } from "@/lib/metadata";

export const metadata = metaData.workforce;

const WorkForce = () => {
  return (
    <LaborForecasting />
  );
};

export default WorkForce;
